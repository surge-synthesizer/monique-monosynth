DONE
- remove adr presets and replace it with an individual env
- fix: wrong step captions:  from 1; 1; 2;... to 1; 2; 3; -> funny bug -> i'm an idiot :D
- force sync if tune is same as osc 1
- add filter pan arangement
- add full stereo processing (fx, eq after filters)
- removed two bands of the eq and changed the frequencys of the others
- fix osc sync problems
- fix velocity glide
- fixed: aliasing bug in osc sync
- fix: click in oscilators
- add: values on global glide times
- add: snap to min and max like to snap to zero
- fm freq max: x8 master osc
- fm swing max: 20hz
- removed osc pulse and switch options
- improved: gain throug the whole synth
- removed: filter boost and clipping and replaced with shape
- bundled filter clipping and distoprtion in one dial
- changed the effect "SHAPE" algorythm
- reworked eq
- reworked filters
- improved: filter input gain: the first input defines up to 100% gain, the second and third shares this amount and keep always max input sum : 1
- optimize signal flow from filter before into the next
- change the position of the final volume
- swap effect flow to chorus, delay, reverb
- removed: tune for osc1 and replace with a phase shift option
- add note offset as second slider of octave offset and a reset button to send a midi note to pitch the arp
- add buttons on top of the filter inputs to show possible inactive inputs
- fix: env popup position
- fix: oscilloscope shot mono mix of flt_1, flt_2, flt_3 and main output instead of only the left channel 
- fix: fx bypass (the reverb was gaining the output by 2)
- changed/fix: process envelopes on the inputs before the filter instead after filter
- removed: option to retune oscs on soft pedal down -> this option is not always on if the softpedal is down
- removed: softpedal changes the gain of an oscilator
- change the num steps for envelope parameters to the maximum sum of ms (20000)
- improve dial: wave, if you move the slider by mousewheel, you increase of decrease to the next clean wave (square, sine, saw or noise)
- UI shows the most values with one number after the comma (coz this is the standard wheel move precessision)
- change: linear env sliders movement no more absolute
- improve: autoclose env popup, but keep it open if you edit another input of the same filter
- add: auto switch and close options to the env popup




miete martin


BUGS:
ulkige frage ob speichern beim wählen eines morph preset (r 4)

UI
add esc option to the env popup
env has no sustain level?
mouse tribble click
env ansicht propotional und zeitachse -> better feeling
gainmeter von links nach rechts
fix slider popup descriptions
font for tooltip misses top stripe
schatten für popup
option: always start in ctrl mode
farbe borders grau
add: a layer with help information for the main ui (Also explain a dual slider quick)
- improve oscilloscope performance

welcome popup, if you like to disable the arp -> show the button

ENGINE
filter modulation off cutoff cracklse (not smoothed?)
lfo sync smoother
phase modulation schwabbelt rum! square und saw -> limit the osc output to 1 and -1 
modulation slider: instead of percent, set the target value!
add back global env shape
bug load or save wave
add: panic
midi standard impl
assertion on shutdow
own lfos for tune and phase

OPTIMIZE
- calculate glide on value change?

LOAD SAVE
wenn ein knopf gedrückt wird, wird das backup nicht aktualisiert (test - SHOT)
bug: create new programm and store it -> then change something and you get the parent program name
- programm load loads bank (plugin bug)

PROGRAMS
- add: a good default program or settings to start with monique (e.g. filter inputs not zero)
- make a starter for percussions

BUILD:
- audio unit build
- asio suppoert



QUESTS:
- get your pro version free for an feature request which we will add to the pro version
- load save hoch -> ???

-------------------------------------------------------------------

mic rechner


LIVE

jackqi
schlüssel womo
moogulator

RUFUMLEITUNG
EMAIL DELAY

set online: audiobus
summer special

// B- STEP

OTHER TODO:
- conTroller in manual



metronom zahlen
metronom typ facebook

-------------------------------

bücher

- leben als haustiere von frauen
- leben als haustier von ausserirdischen, wird jedem sein lebensraum gestallted (artgerecht)

-------------------------------



PRRRRRROOOOOOOOOOOOOOO
- automatisieren von fm frequence!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!! --> super geil

- scala für sequencer
automate fm frequency
- double precessission
- create 256 and 128 and 512 optimized loops
- osc sync cycles
- pan für eq
- pan for reverb and other effects
- snap value smooths to the beat
- 3D pan
- morph between filters
- virtual port support
- ports inside a plugin
- channel management
- only show the real active keys on the midi keyboard (1 or 3 if in osc tune mode)
- play the notes on release which are still down
- add breath
- channel pressure, after touch
- pithch detect output option to assign the synt back to the keyboard
- lfo sync as note offset to the current root note
- add-back set from current for all
- add-back: morph toggles
- midi option (channel, note support)
- advanced settings: osci, adsr presets
- advanced settings: morph
- advanced outputs: curves
- digital analog filter selection
- sequencer
- 3 filter arp buttons, not only one
- asio support
- open an editor to define a pattern if steps for a step
- add: keyboard arrow up/down, left/right to control slider values
- record to file standalone
- osc open sound control
- wind support